{
  "name": "@moruk/ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI services with Apple Intelligence and Gemini fallback",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "bun run lint:fix",
    "lint:fix": "eslint src/ --fix",
    "type-check": "tsc --noEmit",
    "test": "jest --forceExit",
    "test:coverage": "jest --coverage --forceExit",
    "test:watch": "jest --watch",
    "validate": "concurrently --kill-others-on-fail -n \"TypeCheck,Lint,Test\" -c \"magenta,cyan,yellow\" \"bun run type-check\" \"bun run lint\" \"bun run test\"",
    "clean": "rm -rf dist",
    "doctor:fix": "exit 0",
    "fix": "bun run lint:fix && bun run format:fix && bun run doctor:fix",
    "format": "bun run format:fix",
    "format:fix": "npx prettier --ignore-path ../../.prettierignore --write . --log-level error"
  },
  "dependencies": {
    "@moruk/gemini-client": "*",
    "decimal.js": "*"
  },
  "devDependencies": {
    "@types/jest": "*",
    "concurrently": "*",
    "eslint": "*",
    "jest": "*",
    "prettier": "*",
    "ts-jest": "*",
    "typescript": "*"
  },
  "packageManager": "bun@1.3.8"
}
