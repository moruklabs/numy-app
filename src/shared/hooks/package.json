{
  "name": "@moruk/hooks",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "yarn run lint:fix",
    "lint:fix": "eslint src/ --fix",
    "type-check": "tsc --noEmit",
    "validate": "concurrently --kill-others-on-fail -n \"TypeCheck,Lint,Test\" -c \"magenta,cyan,purple\" \"yarn run type-check\" \"yarn run lint\" \"yarn run test\"",
    "clean": "rm -rf dist",
    "test": "jest --passWithNoTests --silent",
    "doctor:fix": "exit 0",
    "fix": "yarn run lint:fix && yarn run format:fix && yarn run doctor:fix",
    "format": "yarn run format:fix",
    "format:fix": "npx prettier --ignore-path ../../.prettierignore --write . --log-level error"
  },
  "dependencies": {
    "@moruk/image": "*",
    "@moruk/logger": "*",
    "@moruk/storage": "*"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "react-native": ">=0.70.0"
  },
  "devDependencies": {
    "@testing-library/react-native": "*",
    "@types/react": "*",
    "concurrently": "*",
    "eslint": "*",
    "jest": "*",
    "jest-expo": "*",
    "prettier": "*",
    "react": "*",
    "react-test-renderer": "*",
    "typescript": "*"
  },
  "packageManager": "yarn@4.12.0"
}
