[project]
name = "claude-hooks"
version = "1.0.0"
description = "Claude Code hooks for monorepo"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
]

[tool.pylint]
max-line-length = 100

[tool.black]
line-length = 100
target-version = ["py311"]

