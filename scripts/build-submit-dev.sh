# numy
# numy
# pet-doctor
# numy
# finance-dictionary
# insect-identifier
# interval-timer
# minday
# numy
# plant-doctor
# rizzman
# stone-identifier

just build-submit numy dev -i;
just build-submit numy dev -i;
just build-submit pet-doctor dev -i;
just build-submit numy dev -i;
just build-submit finance-dictionary dev -i;
just build-submit insect-identifier dev -i;
just build-submit rizzman dev -i;
just build-submit stone-identifier dev -i;



just build-submit interval-timer dev -i;
just build-submit minday dev -i;
just build-submit numy dev -i;
just build-submit plant-doctor dev -i;
